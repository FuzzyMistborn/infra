services:
  matter-server:
    image: ghcr.io/matter-js/matterjs-server:1.2.4
    container_name: matter-server
    restart: unless-stopped
    network_mode: host
    volumes:
      - "{{ appdata_path }}/matter-server/data:/data/"
    security_opt:
      - no-new-privileges:true
