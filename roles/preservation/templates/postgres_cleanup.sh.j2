#!/bin/bash

docker exec -i invidious-db /usr/local/bin/psql invidious -U kemal -c "DELETE FROM nonces * WHERE expire < current_timestamp"
docker exec -i invidious-db /usr/local/bin/psql invidious -U kemal -c "TRUNCATE TABLE videos"